# 设计稿更新报告 2026-07-12

**设计稿**: `网球名片小程序 - UIUX设计稿`  
**ARDot fileId**: `700666824774947`  
**更新目标**: 将上一轮已落地的三项体验改造（TabBar 降高 / 常驻发起比赛按钮 / 排行页重构）回写至设计稿，使设计文档与前端实现保持一致。

---

## 一、TabBar 高度降低（提升屏效）

### 设计稿改动
- 三个 Tab 页（我的名片、战绩大厅、排行榜）的 TabBar 统一调整：
  - 外层高度：83px → **54px**
  - 外层 `y`：761 → **790**（屏幕高度 844 时底部对齐）
  - 内部 tab 项高度：43px → **42px**
  - 内部 tab 项 `y`：20 → **6**

### 与代码对齐
- 代码新增 `client/src/utils/layout.ts` 导出 `TABBAR_HEIGHT = 52`，组件内层渲染高度 50px。
- 设计稿的 54px 视觉高度与代码预留空间一致，整体屏效提升约 35%。

---

## 二、常驻「发起比赛」悬浮按钮（FAB）

### 设计稿改动
- 在三个 Tab 页（`3:1`、`3:124`、`3:223`）各新增一个 position:absolute 的霓虹绿胶囊按钮：
  - 位置：右下角，`x = 224`，`y = 718`，`width = 150`，`height = 56`
  - 样式：圆角 28px，填充 `#D4F820`，深色文字 `#0A0E17`，带霓虹绿阴影
  - 内容：`＋ 发起比赛`
- 删除首页 scroll 内的冗余大按钮（节点 `3:84`），避免与 FAB 重复。

### 与代码对齐
- 代码新增 `components/create-fab/index.vue`，固定定位 `bottom: calc(TABBAR_HEIGHT + safe-area + 16px)`，点击跳转 `/pages/match/create`。
- 设计稿中 FAB 位于 TabBar 之上、不随页面滚动，与代码行为一致。

> **范围说明**：按代码实现，FAB 仅挂在三个 Tab 浏览页。比赛流程页（create / confirm / score-entry）与测评页（assessment）为有固定底部操作区的临时流程，未挂 FAB 以避免重叠；如业务要求字面“每页”，可另行补挂。

---

## 三、排行页重构

### 1. 右上角显示内容与小程序胶囊按钮重叠修复
- 原设计：标题右侧显示「上海徐汇 · Top 100」，与小程序右上角胶囊按钮同高重叠。
- 修改：将位置信息改为标题下方的**左对齐副标题**「上海徐汇 · 好友积分榜」。
- 改动节点：`PageHeader`（`3:241`）由 horizontal 改为 vertical，删除原右侧文本 `3:243`，新增 Subtitle 文本节点。

### 2. 榜单 Tab 收敛为两个
- 删除「全国」Tab（节点 `3:249`）。
- 保留「同城」和「好友」两个 Tab：
  - 「同城」：非高亮，深灰背景，灰色文字（未实现，运行时显示占位提示）。
  - 「好友」：高亮，霓虹绿背景，深色文字（默认选中，展示真实榜单数据）。
- 分段控件宽度重分配：两 Tab 各 171px，居中均分。

### 3. Top3 与榜单防右溢出
- 设计稿保持原有 Podium 三卡片结构（flex-1 均分），截图验证无右侧溢出。
- 代码侧已补充 `flex-1 min-w-0` + 昵称 `truncate`、分数 `flex-shrink-0`，确保长昵称不会撑破右边界。

---

## 四、验证结果

对三页分别执行 `capture_screenshot` 终验：

| 屏幕 | 验证项 | 结果 |
|---|---|---|
| 我的名片 | TabBar 降高、右下 FAB、无 scroll 内 CTA | ✓ 通过 |
| 战绩大厅 | TabBar 降高、右下 FAB | ✓ 通过 |
| 排行榜 | 两 Tab（好友高亮）、标题+副标题左对齐、Top3 与榜单无溢出 | ✓ 通过 |

截图已保存至项目目录 `.workbuddy/screenshots/`：
- `screenshot(3_1)-20260712_094526.webp`
- `screenshot(3_124)-20260712_094526.webp`
- `screenshot(3_223)-20260712_094526.webp`

---

## 五、后续可继续优化项

1. **FAB 覆盖范围**：当前仅 Tab 浏览页挂载；若要求比赛流程页/测评页也有常驻发起按钮，可进一步补挂。
2. **同城榜单占位态**：设计稿目前用静态好友榜展示默认态；可在「同城」选中态补充一个占位画板，显示「同城榜单功能开发中」的图标+文字，与代码完全对齐。
3. **Create / Room 页面拆分**：设计稿中 Create 与 Room 为两页，代码仍为单页 `create.vue` 兼任，属结构性偏差，后续可拆分。
