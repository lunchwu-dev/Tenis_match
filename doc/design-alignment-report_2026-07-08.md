# 设计稿 → 前端代码视觉/交互一致性修复报告

**日期**: 2026-07-08
**设计稿**: ARDot 网球名片小程序 UIUX 设计稿 (fileId: 700666824774947)
**验证**: vue-tsc 类型检查通过（零错误）

---

## 修复概要

基于 7 张设计稿页面截图，逐页比对前端 Vue 实现，发现 **35+ 处不一致项**，按 P0/P1/P2 分级后全部修复完成。

## 修改文件清单

| 文件 | 修改类型 | 关键变更 |
|---|---|---|
| `client/src/pages/index/index.vue` | 视觉重构 | 头部简化、积分单卡、雷达独立、动态数据 |
| `client/src/pages/history/index.vue` | 视觉+交互 | 黄色待确认条、筛选按钮、积分变化、比分格式 |
| `client/src/pages/leaderboard/index.vue` | **P0 数据修复** + 视觉 | 分数整数化、3 Tab、奖牌 Top3、我的排名行 |
| `client/src/pages/assessment/index.vue` | 视觉细节 | 黄色进度条、Q 题号、A/B/C/D 字母、黄色选中态 |
| `client/src/pages/match/score-entry.vue` | 视觉+交互 | 对阵信息头部、我方/客队标签、霓虹绿提交按钮 |
| `client/src/pages/match/confirm.vue` | 微调 | 返回箭头 ‹ |
| `client/src/pages/match/create.vue` | 微调 | 返回箭头 ‹ |
| `client/src/components/tabbar/index.vue` | 图标修正 | ⊞/☰/🏆 |

## P0 必修项（已修复）

1. **排行榜分数显示错误**: `entry.score.toFixed(1)` 将 1847 显示为 18.4 → 改为 `Math.round(entry.score)` 整数显示
2. **首页战绩摘要标签不准确**: "当前连胜"硬编码为"3" → 改为"近期状态"动态计算连胜/连败

## P1 重要修复（已修复，共 ~25 处）

### 首页 (5 处)
- 移除分享按钮 (↗) 和角色标签 ("IT 搬砖人")
- 用户副标题改为 "NTRP X · 性别"
- 积分区从双段式改为单卡（综合积分/ELO/NTRP 进度条/+24▲）
- 雷达图从头部内嵌改为独立卡片（"能力雷达 / 详情 ›"）
- 按钮文字 "发起比赛录入" → "发起比赛"

### 战绩大厅 (6 处)
- 待确认提示条：红→黄 (bg-yellow-500/10)
- 待确认详情：显示对手名+比分+相对时间
- 右上角添加筛选按钮 (≡ 图标)
- 标题下添加 "近期比赛" 副标题
- 历史卡片：添加积分变化 (^+18 / v-12)
- 比分格式：去药丸包裹，大号纯数字
- 头像：L(loser)/C 圆形字母标记替代图片头像

### 排行榜 (7 处)
- Tab: 2 个→3 个（同城/好友/全国），样式改胶囊式
- 添加地名副标题 "上海徐汇 · Top 100"
- Top 3：奖牌式大卡片（渐变底色+巨型数字背景）
- 我的排名：从 Banner 改为简单行（Top 3 下方）
- 副标题：段位·胜率 → NTRP X.X
- 列表过滤：排除 Top 3 和当前用户避免重复

### 能力测评 (7 处)
- 进度条颜色：霓虹绿 → 黄 (#FACC15)
- 顶部栏：‹返回+"能力定级" → ×关闭+题数进度文本
- 题目区：增加 Q X 居中大字 + 类别副标题
- 选项：A/B/C/D 圆形字母前缀
- 选中态：霓虹绿 → 黄绿色高亮
- 移除 "+X 分" 标签
- 按钮："完成评估" → 统一 "下一题"

### 比分录入 (4 处)
- 标题："录入赛果" → "提交比分"
- 返回箭头：✕ → ‹
- 新增对阵信息头部（对阵/A vs B / 单打 · 场地）
- 我方/客队标签在姓名旁
- 提交按钮：白底 → 霓虹绿 (#D4F820)

### 全局 (2 处)
- TabBar 图标：⊞/⚔/# → ⊞/☰/🏆
- 页面返回箭头统一为 ‹（确认页、准备室页）

## 未处理项（留待后续）

### 架构级改进（需较大重构）
1. **Create 与 Room 页面拆分**: 设计稿中是两个独立页面（Create=发起比赛含场地选择，Room=准备室含二维码），当前前端只有一个 create.vue 兼任。建议拆分为：
   - 新建 `pages/match/create.vue` — 发起比赛（模式选择+场地选择+创建按钮）
   - 重构现有 `pages/match/create.vue` 为 Room — 准备室（等待提示+二维码+A队席位+开启比赛）

2. **准备室二维码区域**: 设计稿有完整的二维码邀请区（白底卡片 + QR 占位 + "长按识别二维码 · 邀请码 #8271"），当前实现仅有复制邀请码功能。

3. **确认赛果页设计稿对照**: 该页面在设计稿中可能位于 States/Modals 节点内，本次未截取到独立截图，仅做了返回箭头修正。
